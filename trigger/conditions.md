---
title: 条件
---
`
# 条件

在每个触发器中，可以通过编写条件来决定是否执行这个触发器

条件写在触发器的 `conditions` 列表中，条件列表中每个字符串是一个条件表达式，多个条件之间隐式使用 `&&` 连接（全部满足才通过）。

## 写法

在条件中，你只能使用返回值为Bool(布尔)类型的表达式，否则将报错

### 示例

```yaml
conditions:
  - 'perm("craftorithm.vip")'
  - 'level >= 10'
  - 'world("world_nether")'
```